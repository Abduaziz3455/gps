# Server
NODE_ENV=production
PORT=8090

# GPS API
API_ORIGIN=https://baku.gps.az

# Auto-login credentials (injected into the frontend at serve time)
GPS_LOGIN=
GPS_PASSWORD=

# CORS — comma-separated allowed origins (use * only for development)
CORS_ORIGINS=https://tbbdispatcher.uz
